name: flutter_spike_state_management
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # flutter_riverpod: ^2.1.3
  go_router: ^12.0.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  mockito: ^5.3.2
  build_runner: ^2.3.3

flutter:
  uses-material-design: true